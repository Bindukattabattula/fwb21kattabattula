extends layout

block content
  h1= title
  - var image_names=["a.jpg","b.jpg","c.jpg","d.jpg","e.jpg"]
   - for(let i=0; i<3; i++) {
        - var photos= Math.floor(Math.random()*5)
           img(src=`/images/${image_names[photos]}` alt=`${img}` width='300px' height='300px')
   - }